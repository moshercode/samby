specVersion: "v1alpha"
serviceId: samby-database-service
location: europe-southwest1
schema:
  source: ./schema
  datasource:
    postgresql:
      database: samby-database
      cloudSql:
        instanceId: samby-database-instance
connectorDirs: ["./connector"]

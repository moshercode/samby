query GetBandByFounderEmail($founderEmail: String!) @auth(level: USER) {
  band(first: { where: { founderEmail: { eq: $founderEmail } } }) {
    id
    name
    shortName
    subdomain
    logoUrl
    iconUrl
    primaryColor
    secondaryColor
    founderEmail
    requireIdDoc
    requireIdDocImage
    requireGuardian
    active
    createdAt
  }
}

query GetBandBySubdomain($subdomain: String!) @auth(level: PUBLIC) {
  band(first: { where: { subdomain: { eq: $subdomain } } }) {
    id
    name
    shortName
    subdomain
    logoUrl
    iconUrl
    primaryColor
    secondaryColor
    founderEmail
    requireIdDoc
    requireIdDocImage
    requireGuardian
    active
    createdAt
  }
}

query GetMember($memberId: UUID!) @auth(level: USER) {
  member(key: { id: $memberId }) {
    id
    band { id }
    email
    phone
    role
    status
    isBlocked
    isFounder
    createdAt
    name
    birthDate
    idDoc
    idDocImageUrl
    profileImageUrl
    guardianName
    guardianIdDoc
    guardianIdDocImageUrl
    signatureUrl
    conditionsAcceptedAt
    minorConditionsAcceptedAt
    internalNotes
    requestedAt
    resolvedAt
  }
}

query GetBandConditions($bandId: UUID!) @auth(level: PUBLIC) {
  bandConditions(where: { bandId: { eq: $bandId } }, orderBy: [{ sortOrder: ASC }]) {
    id
    type
    content
    sortOrder
  }
}

query GetBandMembers($bandId: UUID!) @auth(level: USER) {
  members(where: { bandId: { eq: $bandId } }, orderBy: [{ createdAt: DESC }]) {
    id
    email
    phone
    role
    status
    isBlocked
    isFounder
    createdAt
    name
    birthDate
    idDoc
    idDocImageUrl
    profileImageUrl
    guardianName
    guardianIdDoc
    guardianIdDocImageUrl
    signatureUrl
    conditionsAcceptedAt
    minorConditionsAcceptedAt
    internalNotes
    requestedAt
    resolvedAt
  }
}

query GetBandEvents($bandId: UUID!) @auth(level: USER) {
  events(where: { bandId: { eq: $bandId } }, orderBy: [{ createdAt: DESC }]) {
    id
    title
    description
    imageUrl
    startDate
    endDate
    status
    freeEntry
    entryCondition
    updatedAt
    createdAt
    eventMembers_on_event {
      status
      member { id }
    }
  }
}

query GetEventDetail($eventId: UUID!) @auth(level: USER) {
  event(key: { id: $eventId }) {
    id
    title
    description
    imageUrl
    startDate
    endDate
    status
    freeEntry
    entryCondition
    updatedAt
    createdAt
    eventAppointments_on_event(orderBy: [{ eventDate: ASC }]) {
      id
      title
      description
      eventDate
      endDate
      createdAt
    }
    eventMembers_on_event {
      id
      status
      member { id name }
    }
  }
}

query GetMemberEvents($memberId: UUID!) @auth(level: USER) {
  eventMembers(where: { memberId: { eq: $memberId } }) {
    event {
      id
      title
      description
      imageUrl
      startDate
      endDate
      status
      freeEntry
      entryCondition
      updatedAt
      createdAt
    }
  }
}

query GetEventMember($eventId: UUID!, $memberId: UUID!) @auth(level: USER) {
  eventMembers(
    where: { eventId: { eq: $eventId }, memberId: { eq: $memberId } }
    limit: 1
  ) {
    id
    status
    requestedAt
    resolvedAt
  }
}

query GetEventAppointmentDetail($appointmentId: UUID!) @auth(level: USER) {
  eventAppointment(key: { id: $appointmentId }) {
    id
    event { id title }
    title
    description
    eventDate
    endDate
    createdAt
    eventAppointmentComments_on_eventAppointment(orderBy: [{ createdAt: ASC }]) {
      id
      author { id name profileImageUrl }
      content
      imageUrls
      parentComment { id }
      createdAt
    }
  }
}
